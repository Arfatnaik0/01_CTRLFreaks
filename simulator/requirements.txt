# HTTP Requests for API communication
requests>=2.31.0

# Data generation and simulation
numpy>=1.24.0

# Standard library imports (no installation needed)
# logging, threading, time, random, json are built-in Python modules